name: Trigger FB Post
on:
  schedule:
    - cron: '*/5 * * * *' #*/5 7-22 hours of the day, every 5 minutes
permissions: read-all
jobs:
  job1:
    name: Debug
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Run
        run: |
          npm install node-fetch
          node public/cron/server.mjs
