name: Trigger FB Post
on:
  schedule:
    - cron: '*/5 * * * *'
    #- cron: '1 9-17 * * *'ddd
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Post 🔧
        run: date
